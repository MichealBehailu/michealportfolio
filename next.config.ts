import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["screenshots.codesandbox.io", "prowess.org.uk", "thumbs.dreamstime.com", "encrypted-tbn3.gstatic.com"],
  },
};

export default nextConfig;
